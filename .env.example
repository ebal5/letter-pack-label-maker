# レターパックラベル作成 環境変数設定例
# このファイルをコピーして .env を作成し、適切な値を設定してください

# Flask セッション用のシークレットキー
# 必ず変更してください！以下のコマンドで生成できます:
#   openssl rand -hex 32
# または
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Webサーバー設定（オプション）
# HOST=127.0.0.1
# PORT=5000
# DEBUG=false
